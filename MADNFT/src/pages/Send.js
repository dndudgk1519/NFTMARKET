import React from 'react';
import { Button } from '@chakra-ui/react';

function Send() {
  return (
    <div>
      선물하기
      <Button colorScheme="teal" size="md">
        Sending
      </Button>
    </div>
  );
}

export default Send;
