import styled from 'styled-components';

const InnerLayout = styled.div`
  padding: 4rem 15rem;
`;

const SectionStyled = styled.section`
  padding: 4rem 0;
`;

const SmallLayout = styled.section`
  padding: 2rem 7.5rem;
`;
export { InnerLayout, SectionStyled, SmallLayout };
